.worktrees/
.assets
.docs
.env
*.pyc
dist/
build/
*.egg-info/
*.egg
*.pycs
*.pyo
*.pyd
*.pyw
*.pyz
*.pywz
*.pyzz
.venv/
venv/
__pycache__/
poetry.lock
.pytest_cache/
botpy.log
nano.*.save
.DS_Store
uv.lock

# Memory palace database (active one is in nanobot-config)
memory_palace/

# === Added during 2026-06 hygiene & security cleanup ===
# Runtime logs (SECURITY: may contain prompts, memory, tokens, PII)
*.log
nanobot.log
sillytavern.log
gateway.log
# Editor / backup files (never commit)
*.bak
*.bak.*
*~
# JS / frontend build bloat (webui is source for nanobot/web/dist assets)
node_modules/
webui/node_modules/
webui/dist/
# Root dev / test artifacts not part of the package
/a.sh
/fix_*.py
/test_*.py
/test_*.sh
/test_interrupt_sender.py
/test_mp*.py
# Python / tool caches (defensive; some already listed)
.ruff_cache/
.mypy_cache/
.pytest_cache/
__pycache__/
*.pyc
# Submodule noise
Memory-Palace/
config/  # duplicate of Memory-Palace (see .gitmodules)
SillyTavern/  # large third-party; keep if you really need the full tree in this repo

# Keep nanobot/web/dist/ (the *built* web assets that ship with the agent)
# but never the source webui/ node_modules.
